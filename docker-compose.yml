services:
  youtube-instrumental-bot:
    build: .
    restart: always
    volumes:
      - ./downloads:/app/downloads
      - ./output:/app/output
      - ./config:/app/config:ro
    env_file:
      - .env
